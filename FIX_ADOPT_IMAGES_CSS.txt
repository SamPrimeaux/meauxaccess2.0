/* ============================================
   FIX FOR ADOPT PAGE IMAGES - Natural Aspect Ratios
   ============================================ */

/* Replace the existing .animal-photo CSS with this: */

.animal-photo {
    width: 100%;
    border-radius: 12px;
    overflow: hidden;
    margin-bottom: 18px;
    background: #f0f0f0;
    position: relative;
    /* REMOVED: aspect-ratio: 4 / 3; */
    /* ADDED: Flexible height with min/max constraints */
    min-height: 250px;
    max-height: 400px;
    display: flex;
    align-items: center;
    justify-content: center;
}

.animal-photo img {
    width: 100%;
    height: 100%;
    display: block;
    /* CHANGED: object-fit: cover; → object-fit: contain; */
    object-fit: contain;
    object-position: center;
    /* Preserve natural aspect ratio */
    max-width: 100%;
    max-height: 100%;
}

/* For mobile (480px and below), keep a slightly smaller min-height */
@media (max-width: 480px) {
    .animal-photo {
        min-height: 220px;
        max-height: 350px;
        /* Optional: You can keep 1:1 on mobile if preferred */
        /* aspect-ratio: 1 / 1; */
    }
}
